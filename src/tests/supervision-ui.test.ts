import { describe, expect, it } from "vitest";
import { supervisionDaySummary, validateSupervisionImportRow } from "../domain/supervisionUi";
const scope={academicYearId:"2026",gradeId:"2026-2"};const period:any={...scope,id:"p1",name:"\uC790\uC2B5 1\uAD50\uC2DC",active:true,operatingDays:[0,6],startTime:"18:00",endTime:"18:50"};const group:any={...scope,id:"g1",displayName:"A",active:true};const teacher:any={...scope,uid:"t1",displayName:"\uAE40\uAD50\uC0AC",active:true,role:"teacher"};
describe("supervision UI helpers",()=>{it("counts assigned and missing calendar slots including Sunday",()=>expect(supervisionDaySummary("2026-09-20",[period],[group],[{...scope,id:"edge-1",groupId:"g1",periodId:"p1",active:true}],[])).toEqual({operatingPeriods:1,assigned:0,missing:1}));it("accepts a valid import row",()=>expect(validateSupervisionImportRow({date:"2026-09-20",period:"p1",group:"g1",teacher:"t1"},{periods:[period],groups:[group],edges:[{...scope,id:"edge-1",groupId:"g1",periodId:"p1",active:true}],teachers:[teacher],date:"2026-09-20"})).toBeNull());it("rejects invalid period and ambiguous teacher",()=>{expect(validateSupervisionImportRow({date:"2026-09-20",period:"bad",group:"g1",teacher:"t1"},{periods:[period],groups:[group],edges:[],teachers:[teacher],date:""})).toBeTruthy();expect(validateSupervisionImportRow({date:"2026-09-20",period:"p1",group:"g1",teacher:"\uAE40\uAD50\uC0AC"},{periods:[period],groups:[group],edges:[{...scope,id:"edge-1",groupId:"g1",periodId:"p1",active:true}],teachers:[teacher,{...teacher,uid:"t2"}],date:""})).toBeTruthy();});});
describe("supervision preview regression",()=>{const input={periods:[period],groups:[group],edges:[{...scope,id:"edge-1",groupId:"g1",periodId:"p1",active:true}],teachers:[teacher],date:""};it("maps a fully assigned calendar and a non-operating day",()=>{const edge={...scope,id:"edge-1",groupId:"g1",periodId:"p1",active:true};const assignment={...scope,id:"a",date:"2026-09-20",periodId:"p1",selfStudyGroupId:"g1",teacherUid:"t1",teacherDisplayName:"\uAE40\uAD50\uC0AC",active:true};expect(supervisionDaySummary("2026-09-20",[period],[group],[edge],[assignment] as any)).toEqual({operatingPeriods:1,assigned:1,missing:0});expect(supervisionDaySummary("2026-09-21",[period],[group],[edge],[])).toEqual({operatingPeriods:0,assigned:0,missing:0});});it("rejects invalid date, non-operating period, group and missing teacher",()=>{expect(validateSupervisionImportRow({date:"bad",period:"p1",group:"g1",teacher:"t1"},input)).toBeTruthy();expect(validateSupervisionImportRow({date:"2026-09-21",period:"p1",group:"g1",teacher:"t1"},input)).toBeTruthy();expect(validateSupervisionImportRow({date:"2026-09-20",period:"p1",group:"bad",teacher:"t1"},input)).toBeTruthy();expect(validateSupervisionImportRow({date:"2026-09-20",period:"p1",group:"g1",teacher:"bad"},input)).toBeTruthy();});it("requires a clean preview before commit",()=>{const rows=[null,"\uC911\uBCF5 \uD589"];expect(rows.some(Boolean)).toBe(true);expect([null,null].some(Boolean)).toBe(false);});});
